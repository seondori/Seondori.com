name: RAM Price Crawler (Cookie-Based.verX)

on:
  schedule:
    - cron: '5 1 * * *'   # 10:05 KST
    - cron: '5 4 * * *'   # 13:05 KST
    - cron: '5 9 * * *'   # 18:05 KST
  workflow_dispatch:

jobs:
  crawl:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install Chrome browser
        uses: browser-actions/setup-chrome@v1
        with:
          chrome-version: stable

      - name: Install Python dependencies
        run: |
          pip install selenium requests webdriver-manager

      - name: Run crawler
        env:
          NAVER_COOKIES: ${{ secrets.NAVER_COOKIES }}
          GITHUB_ACTIONS: true
        run: |
          cd backend
          python crawler_cookie_based_fixed.py

      - name: Commit and push changes
        if: success()
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action Bot"
          
          # [핵심] JSON 파일과 함께 강제 갱신용 텍스트 파일 생성
          date > backend/last_run.txt
          git add -A
          
          # 변경사항이 있든 없든 무조건 커밋 진행
          git commit --allow-empty -m "🤖 자동 업데이트: RAM 시세 $(date +'%Y-%m-%d %H:%M')"
          git push

      - name: Trigger Vercel deployment
        if: success()
        run: |
          curl -X POST "https://api.vercel.com/v1/integrations/deploy/${{ secrets.VERCEL_DEPLOY_HOOK }}"    

      - name: Upload logs on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: crawler-logs-${{ github.run_id }}
          path: backend/
          retention-days: 7
