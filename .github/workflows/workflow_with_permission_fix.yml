name: RAM Price Crawler (Cookie-Based)

on:
  schedule:
    - cron: '0 1 * * *'   # 한국시간 10:00
    - cron: '0 4 * * *'   # 한국시간 13:00
    - cron: '0 9 * * *'   # 한국시간 18:00
  workflow_dispatch:

jobs:
  crawl:
    runs-on: ubuntu-latest
    # ⭐ 권한 설정 (GitHub Actions가 push할 수 있도록)
    permissions:
      contents: write
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          # ⭐ 중요: 기본 token 사용 (권한 자동 포함)
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install latest Chrome
        uses: browser-actions/setup-chrome@v1
        with:
          chrome-version: stable

      - name: Install Python dependencies
        run: |
          pip install selenium webdriver-manager requests

      - name: Check Chrome version
        run: google-chrome --version

      - name: Run crawler
        env:
          NAVER_COOKIES: ${{ secrets.NAVER_COOKIES }}
          GITHUB_ACTIONS: true
        run: |
          cd backend
          python crawler_cookie_based_fixed.py

      - name: Check crawler results
        if: always()
        run: |
          if ls backend/ram_*.json 1> /dev/null 2>&1; then
            echo "✅ 크롤러 결과 파일 확인됨"
            ls -lah backend/ram_*.json
          else
            echo "⚠️ 결과 파일 없음"
          fi

      # ⭐ 수정된 Git commit & push
      - name: Commit and push changes
        if: success()
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action Bot"
          git add -A
          if git diff --staged --quiet; then
            echo "ℹ️ 변경사항 없음"
          else
            git commit -m "🤖 자동 업데이트: RAM 시세 $(date +'%Y-%m-%d %H:%M')"
            # ⭐ https:// 대신 git:// 프로토콜 사용 (권한 문제 해결)
            git remote set-url origin https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}.git
            git push
          fi
      - name: Trigger Vercel deployment
        if: success()
        run: |
          curl -X POST "${{ secrets.VERCEL_DEPLOY_HOOK }}"
      - name: Upload logs on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: crawler-logs-${{ github.run_id }}
          path: backend/
          retention-days: 7

      - name: Notify on success
        if: success()
        run: echo "✅ 크롤러 성공적으로 완료됨"

      - name: Notify on failure
        if: failure()
        run: echo "❌ 크롤러 실행 실패 - GitHub Actions 로그 확인"
